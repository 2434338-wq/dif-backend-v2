ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ com.dif.ApplicationKt.module ]
    }
}
jwt {
    secret = "dif_metepec_secret_2024_hidalgo"
    issuer = "dif.metepec.gob.mx"
    audience = "dif-app-users"
    realm = "DIF Metepec"
}
database {
    url      = "jdbc:mysql://localhost:3306/dif_metepec?useSSL=false&serverTimezone=America/Mexico_City&allowPublicKeyRetrieval=true"
    driver   = "com.mysql.cj.jdbc.Driver"
    user     = "root"
    password = ""
}
